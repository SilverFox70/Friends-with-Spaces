<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <% user = UserProfile.find(@review.guest_id) %>
  <%= "#{user.first_name} #{user.last_name}" %>
</p>

<p>
  <strong>Body:</strong>
  <%= @review.body %>
</p>

<p>
  <strong>Rating:</strong>
  <%= @review.rating %>
</p>

<p>
  <strong>Place:</strong>
  <%= @review.place.name %>
</p>

<%= link_to 'Edit', edit_review_path(@review) %> |
<%= link_to 'Back', reviews_path %>
